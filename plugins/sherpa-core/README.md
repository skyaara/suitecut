# Sherpa audio plugin core

This package contains the shared Sherpa-ONNX Node WebAssembly runtime used by SuiteCut's
family-specific audio plugins. Install a model package such as `@suitecut/audio-vits` instead of
installing this package directly.
